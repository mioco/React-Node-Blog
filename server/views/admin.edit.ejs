<%- include header %>
<form method="post">
  标题：<br />
  <input type="text" name="title" value="<%= post.title %>" /><br />
  标签：<br />
  <% post.tags.forEach(function(tag, index) { %>
    <% if(tag) { %>
      <input value="<%= tag %>" name="tags" />
    <% } %>
  <% }) %>
  正文：<br />
  <textarea name="post" rows="20" cols="100"><%= post.post %></textarea><br />
  <input type="submit" value="保存修改" />
</form>
<%- include footer %>